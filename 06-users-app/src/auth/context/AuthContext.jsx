// Importar la función createContext de React
import { createContext } from "react";

// Crear un nuevo contexto para la autenticación y exportarlo
// Este contexto se usará para compartir datos y funciones relacionadas con la autenticación
export const AuthContext = createContext();
